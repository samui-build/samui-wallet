pre-commit:
  parallel: true
  jobs:
    - name: format
      run: pnpm format
      glob: '*'

    - name: lint
      run: pnpm lint
      glob: '*'

commit-msg:
  parallel: true
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
