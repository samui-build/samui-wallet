{
  "dependencies": {
    "@effect/platform": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260120.0",
    "@types/bun": "catalog:",
    "@workspace/config-typescript": "workspace:*",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  },
  "name": "api",
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "check-types": "tsc --noEmit",
    "deploy": "wrangler deploy --minify",
    "dev": "wrangler dev",
    "postinstall": "[ -f .env.example ] && [ ! -f .env ] && cp -v .env.example .env || true; bun run cf-typegen"
  },
  "type": "module",
  "version": "0.0.0"
}
